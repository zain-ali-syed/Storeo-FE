import React from 'react';
import Layout from './Layout';


const Home = () => {
    return (
        <Layout>
            <div>
                This is the home component
            </div>
        </Layout>

    );
};

export default Home;